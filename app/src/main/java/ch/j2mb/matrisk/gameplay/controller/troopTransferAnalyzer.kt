package ch.j2mb.matrisk.gameplay.controller



/*The troopsTransferAnalyzer checks to which countries troos can be moved from a specific Country.
* The function getLocations takes a Country as input and returns a list of possible countries where troops can be moved to.
* It returns a empty list if there are no countries troops can be moved to or if there are not sufficient troops on the input Country.
* Troops can be moved from one Country to all other countries which are directly or indirectly connected to it and are occupied by the player
*
 */

class troopTransferAnalyzer {

    fun getLocations() {

    }
}