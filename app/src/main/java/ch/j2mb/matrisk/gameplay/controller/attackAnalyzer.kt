package ch.j2mb.matrisk.gameplay.controller


/*The attackAnalyzer checks which countries can be attacked from a specific country.
* The function getTarges takes a country as input and returns a list of possible targets.
* It returns a empty list if no countries can be attacked if there are no enemy neighbours
* or if there are not sufficient troops (<2 troops) on the input country
*
 */
class attackAnalyzer {

    fun getTargets() {

    }
}